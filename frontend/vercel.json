{
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "yarn install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
